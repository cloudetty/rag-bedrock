FROM busybox:1.36.1

LABEL org.opencontainers.image.title="Bedrock access gateway"
LABEL org.opencontainers.image.description="Placeholder container we can build and push from CI."

CMD ["/bin/echo", "Bedrock gateway container placeholder"]
